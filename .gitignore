# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local env files — never commit real secrets.
# .env is currently tracked because Lovable injects VITE_SUPABASE_* at
# build time via the committed file. When switching to per-environment
# secret injection, untrack .env and rely on .env.example only.
.env.local
.env.*.local
